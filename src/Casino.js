
export default function makeBet() { 
    console.log("gamba!");
    //call some contract method 
        //contract method should take bet, determine winner, pay out or not,
        //emit some event indicating if we won or not
}

